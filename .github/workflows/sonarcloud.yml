name: SonarCloud

on:
  push:
    branches:
      - main
      - SHOP-DEVELOP
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies (frontend)
        working-directory: frontend
        run: npm install

      - name: Install dependencies (backend)
        working-directory: backend
        run: npm install

      - name: SonarCloud Scan
        uses: https://github.com/SonarSource/sonarcloud-github-action/commit/ffc3010689be73b8e5ae0c57ce35968afd7909e8
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=thalles-j-key
            -Dsonar.organization=Surface_sonar
            -Dsonar.sources=backend/src,frontend/src
            -Dsonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/.vite/**
